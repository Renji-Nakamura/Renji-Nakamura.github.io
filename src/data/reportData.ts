export interface SectionItem {
  id: string;
  number: number;
  title: string;
  body: string;
}

export const REPORT_META = {
  title: "MetalGroundプロジェクト 全容とシステム解析の軌跡",
  subtitle: "Apple Silicon M3 における Grounding DINO の完全 Exact モード高速化と 54 実験の学術的技術報告書",
  author: "Renji Nakamura",
  affiliation: "岩手県立大学 ソフトウェア情報学部 / Autonomous Systems Research",
  targetHardware: "Apple M3 (16GB Unified Memory)",
  targetModel: "Hugging Face Grounding DINO Tiny (172M parameters)",
  latencyBaseline: "999.12 ms (1.00 FPS, PyTorch MPS)",
  latencyFinal: "476.36 ms (2.099 FPS, MetalGround)",
  speedup: "2.08x End-to-End Speedup",
  accuracyImpact: "+0.00076 AP on COCO 5000 Val (Zero Degradation)",
  totalExperiments: 54,
  portfolioUrl: "https://renji-nakamura.github.io/"
};

export const REPORT_SECTIONS: SectionItem[] = [
  {
    "id": "section-1",
    "number": 1,
    "title": "研究の背景と基本目的",
    "body": "本プロジェクト「MetalGround」の主対象ハードウェアは、16 GBのユニファイドメモリを搭載したApple M3チップ搭載のMacBook Pro 14インチです。最適化の対象とする主ベースラインは「Hugging Face Grounding DINO Tiny (パラメータ数: 172,250,626)」と「PyTorch MPS (Metal Performance Shaders)」バックエンドです。\n\n研究の中心となる問いは、「モデルのアーキテクチャ再設計、再学習、量子化（Quantization）、重みの枝刈り（Pruning）、低精度化（Reduced precision）といった**精度を落とす近似（Approximation）に一切妥協せず**、Grounding DINOそのものの計算とランタイムをApple Siliconに特化させることで、実用的な推論速度まで高速化できるか」という点にあります。この「Exact mode（厳密な最適化）」と、後に検討される「Approximate mode（近似的最適化）」を明確に分離することが、研究全体を貫く鉄則として設定されました。\n\n再現性を担保するため、入力画像は公式デモで用いられる `assets/input.jpg`（SHA-256: `f056b63ab86ac8c83471316351e0df5fcc33ce21160f0e041a12d8a80c4eea8f`）に固定されました。元画像サイズは 1200 x 901 であり、Hugging Face processorを経由することで `pixel_values` が `[1, 3, 800, 1065]`、`pixel_mask` が `[1, 800, 1065]` というテンソルに変換されます。固定プロンプトは `[\"a cat\", \"a dog\"]` （テキストシーケンス長: 8）とされました。"
  },
  {
    "id": "section-2",
    "number": 2,
    "title": "初期ベースラインの確立（Experiment 0001〜0002）",
    "body": "最適化を行う前の信頼できる基準値（ベースライン）を確立するため、Experiment 0001が実行されました。warmupを3回、計測を10回行った結果、PyTorch CPUでのforward median latencyは `2205.66 ms`（平均 `2206.34 ms`、p95 `2241.60 ms`、0.4534 FPS）であったのに対し、PyTorch MPSではforward median latencyが `999.12 ms`（平均 `1010.41 ms`、p90 `1045.65 ms`、p95 `1045.99 ms`、min `987.44 ms`、max `1046.33 ms`、1.0009 FPS）となりました。 MPS化によりCPU比で約 `2.21倍` の高速化が確認されましたが、依然として推論速度は約1 FPSにとどまっていました。さらに、MPSのend-to-end latencyのmedianは `1024.41 ms`（平均 `1029.67 ms`、p95 `1062.07 ms`、0.9762 FPS）であり、forwardとの差はわずか約 `19.26 ms` でした。これにより、「遅延の主因は画像I/Oや前処理・後処理ではなく、モデルのforward計算そのものにある」という重要な作業仮説が導き出されました。なお、MPSのメモリアロケーションは現在の割り当てが `781,274,368 B`、ドライバ割り当てが `2,429,861,888 B` と測定されています。\n\n続くExperiment 0002のモデル構造（インベントリ）解析により、モデル内部には `num_queries = 900`、`encoder_layers = 6`、`decoder_layers = 6`、`d_model = 256` の構造があり、特に Multi-Scale Deformable Attention (MSDA) がエンコーダ側に6個、デコーダ側に6個の計12個存在することが確認されました。"
  },
  {
    "id": "section-3",
    "number": 3,
    "title": "階層的プロファイリングとボトルネックの特定（Experiment 0003〜0005）",
    "body": "999.12 msの遅延要因を突き止めるため、Experiment 0003で階層的なステージプロファイリングが実施されました。非同期実行のMPSを正確に計測するため `torch.mps.synchronize()` を挟んで計測された診断用の中央値は以下の通りです。\n\n- Grounding DINO encoder: `743.69 ms` (全体の約74.4%)\n    \n- Swin / image backbone: `164.65 ms` (約16.5%)\n    \n- Grounding DINO decoder: `71.88 ms` (約7.2%)\n    \n- BERT text backbone: `11.41 ms` (約1.1%)\n    \n\nエンコーダ層（各層 `123–127 ms`）をさらに分解すると、Text enhancer 6層合計は約 `4 ms`（各 `0.65–0.85 ms`）に過ぎませんでしたが、Vision-language fusion 6層合計は約 `180 ms`（各 `29–31 ms`）、Deformable layer 6層合計は約 `554 ms`（各 `91–94 ms`）に達しました。 特にMSDAのラッパー全体（12個）は `465.37 ms`（約46.6%）を占め、その中のサンプリングコア（`MultiScaleDeformableAttention`）単体でも `408.25 ms`（約40.9%）を消費していました。内訳は、エンコーダ側MSDAコアが6個で `384.30 ms`（1個あたり `63–65 ms`）、デコーダ側が6個で `23.95 ms`（1個あたり `3.5–4.8 ms`）です。この圧倒的な差は、エンコーダが `[1, 17821, 8, 32]` という巨大なシーケンス長（17,821ポジション）を処理するのに対し、デコーダは900クエリしか処理しないというテンソルサイズの違いに起因します。Amdahlの法則によれば、MSDAコアの `408.25 ms` を完全にゼロにできたとしても、全体の高速化は `999.12 / (999.12 - 408.25) ≈ 1.69倍` に留まります。しかし、最大の単一ボトルネックであることは間違いなく、最初のカスタム最適化ターゲットとしてエンコーダMSDAが選定されました。\n\nExperiment 0004では、実モデルからキャプチャしたテンソルを用いてMSDAを単体（isolated）でベンチマークしました。エンコーダ入力の総データ量は `45,621,856 B`、出力は `18,248,704 B`（合計約 `63.87 MB`）でした。結果はエンコーダMSDAコアがmedian `69.89 ms`（mean `68.60 ms`, p90 `72.64 ms`, p95 `72.89 ms`, min `60.52 ms`, max `74.56 ms`）、デコーダがmedian `3.54 ms`（mean `3.93 ms`, p90 `4.54 ms`, p95 `4.74 ms`, min `3.39 ms`, max `8.32 ms`）となり、モデル内計測と単体計測で約 `+6.63 ms`（+10.5%）の差が確認されましたが、出力の最大絶対誤差は `0.0` と完全な再現性を確保しました（エンコーダとデコーダのコスト比は約 `19.7倍`）。\n\nExperiment 0005では、この約70 msのMSDA処理の内部コストを分解しました（診断用全体コスト `61.39 ms`）。その結果、4レベルの `grid_sample` 自体のコストは計約 `8.15 ms`（レベル0から順に `3.437 ms`, `1.758 ms`, `1.487 ms`, `1.468 ms`で全体の約13.3%）に過ぎず、サンプリング結果（4レベル合計で `291,979,264 B ≈ 278.45 MiB`）の `stack / flatten` に `21.84 ms`（約35.6%）、さらにアテンション重みの乗算とリダクション、出力レイアウト処理に `31.64 ms`（約51.5%）かかっていることが判明しました。論理的なメモリアクセス総量は `894,186,496 B ≈ 852.76 MiB` に達しており、「ランダムアクセスなサンプリングが遅い」という直感的な仮説は覆され、「巨大な中間テンソルの生成（materialization）とリダクション処理こそが真のボトルネックである」という本質的な課題が浮き彫りになりました。"
  },
  {
    "id": "section-4",
    "number": 4,
    "title": "Metal MSDA v0の開発とDLPackブリッジ（Experiment 0006〜0007）",
    "body": "Experiment 0005の洞察に基づき、Experiment 0006では、中間テンソルを一切生成せず、サンプリングから重み乗算、リダクションまでを1つのカーネルで完結させる「Fused Metal MSDA v0」がMLXの `mx.fast.metal_kernel` を用いて開発されました。 この正当性重視（correctness-first）のFP32実装により、エンコーダMSDAの単体処理時間は `69.892125 ms` から median `5.269 ms`（mean `5.486 ms`, p90 `6.279 ms`, p95 `6.449 ms`, min `4.857 ms`, max `6.463 ms`）へと激減し、約 `13.26倍` の驚異的なスピードアップを達成しました。精度面でも最大絶対誤差 `4.5896e-6`、平均絶対誤差 `1.8652e-8`、RMSE `5.0857e-8`、コサイン類似度 `0.9999999999999336` を記録し、暫定の `allclose(1e-5)` ゲートをクリアしました（相対誤差の最大値は0付近の計算により `0.1316` でしたが平均は `9.6518e-7`）。予測されるフルモデルへのインパクトは、384.299 msが31.614 msに縮小し、全体が約 `646.431 ms`（約1.55倍）になると試算されました。\n\nしかし、モデル本体はPyTorch MPSで動作しているため、フレームワーク間のデータ受け渡しコストが課題となります。Experiment 0007では、DLPackを利用したゼロコピーインポート（`mx.asarray(..., copy=False)`）のテストが行われました。`spatial_shapes` や `level_start_index` といった数十バイトの静的メタデータはMLXのint32としてネイティブ構築し、巨大な計算テンソルのみを共有ストレージ経由で受け渡すアーキテクチャが採用されました。 同期コスト込みの計測でも、エンコーダブリッジは median `5.203 ms`（p95 `7.030 ms`, min `4.964 ms`, max `8.602 ms`）で約 `13.43倍` の高速化を維持。デコーダブリッジも median `0.533 ms`（p95 `0.762 ms`, min `0.499 ms`, max `0.794 ms`）で約 `6.64倍`となり、スタンドアロンでのカーネルの劇的な改善が、フレームワーク間ブリッジを経由しても失われないことが実証されました。デコーダの最大絶対誤差も `3.5763e-7`（RMSE `1.9231e-8`）と極めて高精度でした。"
  },
  {
    "id": "section-5",
    "number": 5,
    "title": "順位付けによる誤差増幅の発見とフルモデル統合（Experiment 0008〜0009）",
    "body": "Experiment 0008では、12個のMSDAコアすべてをMetalGround v0に置き換えるフルモデル統合が行われました。しかし初期テスト（0008a）において、最終的な `pred_boxes` に最大 `0.3023`（median `2.36e-4`, p95 `7.09e-3`, p99 `2.81e-2`, >0.1が7個）の誤差が生じ、`logits` にも max abs `1.577`（mean `0.01468`）の差異が発生しました（`logits` の NaN は単なる `-inf` 同士の比較アーティファクトであり、finite要素は7,200個、`-inf`は223,200個でマスクは完全一致）。\n\nこの数値的崩壊の原因を探るため、0008bで層ごとに誤差を追跡した結果、エンコーダの最終Vision隠れ状態の誤差は max abs `1.97e-6`、`enc_outputs_class` は `1.29e-5`、`enc_outputs_coord_logits` は `4.10e-5` に過ぎないにもかかわらず、デコーダに渡す `init_reference_points` で突如として max abs `0.20369`（mean `5.26e-4`）に跳ね上がることが判明しました（層0単独でも `0.20369`、層5単独でも `0.07913`）。\n\n0008cの監査により、この現象の正体が解明されました。スコアの差はわずか `1.0014e-5` であり、Top-900のメンバーシップ自体は `900/900` で完全一致（Jaccard 1.0、900-901位マージン `2.6345e-4`）していました。しかし、スコアが極めて拮抗している（near-tie）プロポーザルの内部でわずか数個（6スロット）の順位が入れ替わっており（Rankwise identical `894/900`）、ランクベースで異なるバウンディングボックスの座標が比較されたために見かけ上の巨大な誤差として表出していたのです。\n\nこれを因果的に証明したのがExperiment 0008dの介入実験です。ミスマッチしたランク（473/474、794/795、809/810の3組6スロット）について、プロポーザルのIDを揃えて座標誤差を見ると max abs は `2.2054e-6`（mean `1.6711e-7`）と極小でした。そして、Metalエンコーダの出力のままTop-Kの「順序」だけをリファレンスと強制的に一致させると、`init_reference_points`の誤差は `0.203688 -> 2.2054e-6`（約9.24e4倍縮小）、logitsは `1.57697 -> 2.2507e-4`（約7.01e3倍縮小）、boxesは `0.302299 -> 6.5207e-5`（約4.64e3倍縮小）と激減し、1e-4のゲートを通過しました。「浮動小数点の数µレベルの微小な誤差が、離散的なプロポーザルの順位付け（Top-K Selection）の不連続性によって増幅され、学習済みのクエリスロットとのペアリングを変化させる」という、ディープラーニングモデル特有のシステムレベルの挙動が完璧に立証されました。\n\n数値的因果関係が解明されたことで、Experiment 0009で正式なパフォーマンス評価が行われました。12個のMSDAコール（計156回）をMetalGround v0に置き換えたフルモデルの推論レイテンシは、ベースラインの `999.116584 ms` から median `632.675 ms`（mean `632.867 ms`, p90 `638.205 ms`, p95 `642.279 ms`, p99 `645.538 ms`, min `623.782 ms`, max `646.352 ms`）へと短縮され、絶対時間で `366.442 ms`（36.68%）の削減、スピードアップ `1.579倍`（1.581 FPS）を達成しました。固定画像でのpostprocess後の検出スコアも、catが `0.505246 -> 0.505461`、dogが `0.319794 -> 0.319798` とほぼ同一であり、単体ベンチマークの改善がフルモデルにそのまま移植されることが確認されました。"
  },
  {
    "id": "section-6",
    "number": 6,
    "title": "代数的最適化によるFusionレイヤーの高速化（Experiment 0010〜0015）",
    "body": "MSDAの解消によりボトルネックの構造が変化したため、Experiment 0010/0011で再プロファイリングが行われました。新たな中央値はエンコーダ `387.112 ms`、バックボーン `156.399 ms`、デコーダ `57.221 ms`、BERT `10.989 ms`となりました。エンコーダ内では、Fusionレイヤー合計が `178.9 ms`（各層 `28.679`〜`30.958 ms`）、Deformableレイヤーが `220.1 ms` でした。 Fusionコストの大部分は `GroundingDinoBiMultiHeadAttention`（`159.337 ms`）であり、特にVision側の3つのプロジェクション（`vision_proj` `23.821 ms`、`values` `23.912 ms`、`out` `23.339 ms`）だけで `81.898 ms` を消費していました（残余のBMMやsoftmax等は `77.439 ms`）。Deformable側のFFNは `fc1` `45.666 ms`、`fc2` `44.277 ms` で計 `89.94 ms`。MSDAラッパーは `113.763 ms`、コアは `36.832 ms` に縮小し、差分の `76.93 ms`（プロジェクション群 `28.519 ms` 等）が残存していました。この結果から、次の最大の反復ボトルネックはFusion BiMHAであると定量的に特定されました。\n\nExperiment 0012での一般的なMLX移植テストでは、PyTorch `27.490 ms` に対して MLX compiled は `25.702 ms`（1.070倍）と改善が乏しかったため、モデル固有のテンソル形状（Vision 17,821トークン、Text 8トークン、埋め込み次元1024）を利用した代数的なグラフ最適化（Algebraic Specialization）がExperiment 0013で試みられました。 参照の3つの巨大なVisionプロジェクションには約 `14,015,004,672 MAC` が必要ですが、交差アテンションのスコア計算は約 `437,968,896 MAC` に過ぎません。そこで、アテンションリダクションの前後で値（Value）と出力（Output）のプロジェクションを代数的に結合（reassociate）し、Q/Kスコアも双一次形式（bilinear form）へ畳み込む `fully_folded` 変法を実装しました。これにより、PyTorch/MPS上で推論グラフを変更しただけで、レイテンシは `27.687 ms` から `15.837 ms` へと劇的に低下（1.748倍）。重みを一切変更せず、4つの出力系統すべてが暫定の1e-5ゲートを通過しました（`value_folded` は `25.058 ms`で1.105倍）。\n\nExperiment 0014でこれを6層すべてに統合した結果、フルモデルのレイテンシは `fully_folded` で median `566.274 ms`（p95 `579.815 ms`, p99 `584.371 ms`）を記録し、MSDA-v0単体（同プロセス内で `651.467 ms`）からさらに `66.401 ms`（約10.49%）の削減に成功しました。Top-900メンバーシップは `900/900` で完全保存され、ランクミスマッチは808/809の1組のみ。raw logits max abs `0.3873`、boxes `0.1190` でした。 これを確定させるため、Experiment 0015で「MetalGround v1」としてのオーソリテイティブなフレッシュプロセス・ベンチマークを実行しました。結果は mean `576.284 ms`、median `574.352 ms`（p90 `600.683 ms`, p95 `602.073 ms`, p99 `603.545 ms`, min `553.777 ms`, max `604.119 ms`）。元の `999.117 ms` に対して `1.7396倍` のスピードアップ、絶対時間で `424.764 ms`（42.514%）の削減、`1.741 FPS` を達成しました。固定画像でのpostprocess検出もほぼ一致し、MSDAコールも期待通りの420回でした。"
  },
  {
    "id": "section-7",
    "number": 7,
    "title": "実行アイランドの拡大とDLPack同期バグの発見（Experiment 0016〜0042）",
    "body": "v1状態での再プロファイル（Experiment 0016）により、エンコーダ（`333.757 ms`、58.1%）、バックボーン（`164.588 ms`、28.7%）、デコーダ（`57.705 ms`、10.0%）という構成になり、エンコーダ内はFusion（`91.571 ms`）、FFN+Norm（`99.056 ms`）、MSDAラッパー（`99.511 ms`）の3領域がほぼ同等に並びました。フレームワーク間の同期オーバーヘッドを削減するため、Experiment 0017では `mx.compile` を用いてこれらの処理を1つの大きなMLX実行アイランド（Deformable Island）にまとめました。これによりハイブリッド版の `35.980 ms` から `33.311 ms`（1.080倍、`2.669 ms/層`削減）へと高速化しました（eager版は `37.511 ms`）。\n\nこのコンパイル済みDeformable Islandをフルモデルに組み込んだExperiment 0018（MetalGround v2）では、mean `545.484 ms`、median `542.305 ms`（p90 `556.700 ms`, p95 `568.606 ms`, p99 `578.071 ms`, min `533.151 ms`, max `581.307 ms`）を達成。v1からさらに `32.047 ms`（5.58%）削減し、オリジナル比で `456.811 ms`（45.722%）削減、`1.842倍`（1.844 FPS）となりました。ランクスワップは302/303の1組のみで、catスコア `0.5052461 -> 0.5051671`、dogスコア `0.3197940 -> 0.3198193` でした。コール数もエンコーダ/デコーダ共に `210/210` を記録。\n\n続くExperiment 0019、0020、0021でのプロファイリングとFFNのAPI最適化（3D matmul `21.039 ms`、2D matmul `21.876 ms`、2D addmm `21.206 ms` で改善なし・NO-GO）を経て、Experiment 0030では `fully_folded` Fusionをコンパイル済みMLXへ移行。PyTorchの `15.327 ms` に対し、ブリッジ込みで `6.576 ms`（2.331倍）へと劇的な性能向上（層あたり約 `8.751 ms` の削減）を確認しました。Experiment 0031のフルモデルペア比較でも、v2 `575.340 ms` に対してMLX Fusion候補は `517.927 ms`（1.1109倍、ペア中央値 `+56.027 ms` の改善）を記録。Experiment 0032の5プロセスの反復テストでもペア中央値 `+54.826 ms`（範囲 `50.869 ... 61.133 ms`）と堅牢な結果が得られました。驚くべきことに、MLX Fusion候補はオリジナルに対するランクミスマッチが解消され、Rankwise `900/900` に回帰しました（logits `3.104e-4`、boxes `8.368e-5` のallclose 1e-4）。\n\nさらに境界を排除するため、Experiment 0033でFusionとDeformableを結合したWide Islandをテストし、`43.202 ms`から `39.271 ms`（`3.931 ms` 削減、1.1001倍、9.10%）の改善を確認。しかし、Experiment 0034で6層すべてにWide Islandを導入したところ、レイテンシは `467.962 ms` から `456.899 ms`（ペア中央値 `+11.778 ms`）に改善したものの、数値が深刻に崩壊しました。Top-900のオーバーラップが `864/900` に低下し、36個のプロポーザルが入れ替わり、Rankwise一致はわずか `14/900`（logits最大差 3.19、boxes 0.853）となりました。\n\nこの崩壊原因を特定するため、Experiment 0035で層ごとに同一入力でのローカル誤差を監査しました。その結果、レイヤー0からVision出力の最大誤差が `7.692` と爆発しており（L1 `8.872`、L2 `12.829`、L3 `9.279`、L4 `7.376`、L5 `0.442`）、全層で `allclose(1e-4)` を失敗していることが判明しました。一方でText出力は正常でした。 原因は「同期バグ」でした。PyTorch側で `torch.mps.synchronize()` を呼び出した「直後」に `~key_padding_mask` を生成し、それを `mx.asarray(copy=False)` でMLXに渡していました。DLPackのゼロコピー変換は保留中のMetalの処理を同期しないため、同期後に生成されたマスクがデータ競合を起こし、Visionブランチ（Deformable側）のみをサイレントに破壊していたのです。\n\nExperiment 0036でこのバグを修正し、反転前のマスクをMLXに渡してMLX内で `logical_not` を適用するようにした結果、Vision誤差はレイヤー0で `3.183e-5`（L5で `1.088e-6`）へと激減し、全層で `1e-4` を通過。フルモデルのTop-900メンバーシップも `900/900` に完全復元しました。パフォーマンスもセパレート版 `505.202 ms` に対して修正後Wide版は `489.416 ms`（ペア中央値 `+15.038 ms`、1.0323倍）と改善を維持。Experiment 0037の5プロセス反復テストでもペア中央値 `+14.385 ms`（範囲 `12.368 ... 18.812 ms`）と堅牢な結果を示しました。\n\nその後、Experiment 0038で最適化の累積アブレーション（v2 `557.442 ms` → MLX fusion `510.758 ms` [+52.114 ms] → Wide `496.353 ms` [+13.458 ms] → Wide+Cache `483.471 ms` [+12.700 ms]、合計 +78.294 ms、1.1530倍、13.27%、FPS 2.068）を行い、すべてが加算的に効果を発揮することを確認しました。Experiment 0039のマルチプロセス・ヘッドライン検証では、制御されたv2（プロセス中央値 `567.124 ms`）に対して、統合された最終ランタイムはプロセス中央値 `486.578 ms`（範囲 `483.287 ... 489.220 ms`、FPS `2.055`）を記録。ペア中央値の節約は `+80.226 ms`（範囲 `78.498 ... 82.398 ms`）に達し、1.1655倍（14.20%、`80.547 ms`削減）となりました。Top-900は完全維持され、ランクミスマッチは既知の302/303のみ。これが新たな強固なマイルストーンとなりました。\n\nExperiment 0040での再プロファイリング（エンコーダ `244.332 ms`、バックボーン `170.030 ms`、デコーダ `61.057 ms`、Text enhancer `0.67 - 0.74 ms/layer`）を踏まえ、Experiment 0041/0042でエンコーダ層間を跨ぐ巨大なSuper-Islandの構築が試みられました。2層でのテスト（0041）では `83.670 ms` -> `80.761 ms`（`+2.512 ms`, 1.0360x）と成功しましたが、6層フルモデル（0042）では `484.602 ms` -> `480.209 ms`（ペア中央値 `+4.427 ms`, 1.00915x）にとどまり、事前登録の5 msゲートを満たさなかったため採用見送り（NO-GO）となりました（ランクスワップは302/303と472/473に発生）。"
  },
  {
    "id": "section-8",
    "number": 8,
    "title": "Swin Backboneの微細最適化（Experiment 0022〜0026, 0043〜0049）",
    "body": "バックボーンの最適化も並行して進められました。Experiment 0022ではSwin Stage0のブロック全体をMLXに移植し、Regularブロック（block0）ではPyTorch `23.501 ms` に対してFused-QKVが `23.499 ms`（1.00008倍）と全く差が出ませんでしたが、Shiftedブロック（block1）では `28.872 ms` が `25.755 ms`（1.121倍、`3.117 ms`削減）へと明確に改善しました。 この差がアーキテクチャではなく「シフトマスク生成の静的キャッシュ」によるものだと推測し、Experiment 0023でPyTorchのままマスクをキャッシュしたところ `32.020 ms` -> `28.595 ms`（1.11977倍、`3.425 ms`削減）となり、仮説が立証されました。しかし、Experiment 0024で全Shiftedブロックにキャッシュを適用するとフルモデルで `542.305 ms` -> `565.036 ms` (+22.731 ms, 0.960x) と悪化。Experiment 0025の詳細監査により、Stage0 block1のみが `+2.338 ms`（1.0837x）の恩恵を受け、Stage2等では悪化（-0.663 msなど）することが判明しました。Experiment 0026でStage0単独キャッシュをフルモデルに適用してもペア中央値 `-0.866 ms` とノイズに埋もれたため、マスクキャッシュはNO-GOと判断されました。\n\nExperiment 0043でのプロファイリング（Deform cross-attn `3.86-3.98 ms`、Swin Stage0 MLP `12.176 / 11.602 ms`）を経て、Experiment 0044系列でデコーダラッパーのMLX化を試みましたが、フレッシュな `copy=True` のインポートでコンパイルがストールし、システム統合上の致命的欠陥として放棄されました（永続的インポートの0044eでは `4.779 ms` -> `3.181 ms`、+1.655 ms、1.502倍と優秀でしたが統合不能）。 その後、Swin MLPを局所的にMLXに置換しました。Experiment 0045において、Stage0 block0でPyTorch `12.716083 ms` -> MLX `7.3071875 ms`（`+5.0916665 ms`, 1.7402倍, 42.54%削減）、block1で `12.098500 ms` -> `6.8929165 ms`（`+5.494583 ms`, 1.7552倍, 43.03%削減）という劇的な改善を示し、Experiment 0046（ペア中央値 `+5.7187505 ms`）および0047のマルチプロセス検証（ペア中央値 `+6.0210835 ms`、範囲 `4.318979 ... 10.156562 ms`）を経て正式採用（ROBUST GO / ADOPT）されました。最終的な絶対値オーソリティは `476.360333 ms`（2.099251 FPS）に更新されました。 しかし、続くExperiment 0048でStage 1〜3の残り10個のMLPをMLX化したところ、Stage 1（-2.98%）、Stage 2（-3.74%）、Stage 3（-2.66%）とすべて悪化しました。さらにExperiment 0049でSwinのAttentionのみを置換した場合も、Stage0からStage3まですべて（-52.6% 〜 -1.65%）遅延し、完全にNO-GOとなりました。巨大なテンソル（`[1, 53400, 96]`）ではMLXへのオフロードが機能するものの、次元が小さくなるとブリッジのオーバーヘッドを回収できないという、ワークロード形状に強く依存するバックエンドの境界線（crossover）が極めて明確に示された結果です。"
  },
  {
    "id": "section-9",
    "number": 9,
    "title": "最終パフォーマンスと圧倒的な精度証明（Experiment 0050〜0052）",
    "body": "Experiment 0050では、同一プロトコル下でオリジナルと最終版の直接比較（5プロセス、各8ペア）を実施。ペアの遅延削減中央値のプロセス中央値は `+523.1220825 ms`（範囲 `519.545292 ... 529.3728955 ms`）。速度比のプロセス中央値は `2.0820484749倍`（範囲 `2.0794405043 ... 2.0896722869倍`）という圧倒的なエンドツーエンド・スピードアップが確定しました（オリジナル `1004.1033335 ms` / 0.995913 FPS、最終版 `481.6858125 ms` / 2.076042 FPS）。Top-kメンバーシップは `900/900` で維持され、ランクミスマッチは472/473のみ。スコアベクトルの最大絶対誤差は `1.2397766e-5` でした。 なお、プロンプトキャッシング（Experiment 0027-0029）も事前に実証済みで、キャッシュヒット時にはベースライン `572.192 ms` に対して `561.685 ms`（1.01871倍）、ペア中央値 `+10.303 ms`（平均 `+12.447 ms`）。5プロセス検証でもペア中央値 `+11.911 ms`（範囲 `+6.781 ... +15.930 ms`）を記録し、ロバストに採用されています。ベースライン変動幅（22.543 ms）が最適化効果を上回るノイズフロアの現実も、ペア検証の重要性を裏付けました。\n\nExperiment 0051では、単一画像での検証を超え、33画像×5プロンプトの計165ケースで行動的忠実性（Behavioral sweep）を監査しました。驚くべきことに、Top-kメンバーシップは **165/165 (100%)** で完全に保存されました。Rankwiseが完全に一致したのは41/165（24.85%）に留まりましたが、ミスマッチのほぼすべて（123/124）が隣接スワップであり、生のロジット誤差が最大 4.10824、ボックス誤差が 0.95939 に達するケースがあっても、最終的なデフォルト閾値（0.30/0.25）および厳格な閾値（0.40/0.30）での検出数、ラベルマルチセット、Same-label IoU >= 0.99 の一致率は **165/165** と完全一致しました。Permissive設定（0.20/0.20）での5件の文字列不一致も、たった1つのトークンが `0.200039953 -> 0.199915633`（差 `-1.2432e-4`）のようにハード閾値を跨いだことによる現象と完全解明（Experiment 0051a）。唯一の非隣接例外も `14372, 15998, 11031`（スコア差 `4.2915e-6`）が `11031, 14372, 15998`（スコア差 `3.3379e-6`）へ巡回置換しただけの3-way near-tieであることが突き止められました。\n\nこの強固な基盤の上で、Experiment 0052はCOCO val2017の全5000画像を用いたGround-truth labeled accuracyの直接評価に挑みました。固定Webcamジオメトリ（1200x901）を用いた評価において、オリジナルが `47.97026520 AP` であったのに対し、MetalGround最終版は `47.97103061 AP` を記録。その差はわずか **`+0.00076541 AP points`** であり、事前登録された `0.10 AP` の許容マージンのわずか `0.765%` に収まりました。AP50は `-0.00202349`、AP75は `+0.00095117`、AR smallは `-0.07574952`。カテゴリ単位でも最大の変動はtennis racketの `+0.09332019 AP points` と elephantの `-0.02536471 AP points` に過ぎません。これにより、モデル精度が1ミリも犠牲にされていないことが完璧な数値データとともに証明されました。"
  },
  {
    "id": "section-10",
    "number": 10,
    "title": "リアルタイム・ウェブカムシステムのプロトタイプ検証（Experiment 0053）",
    "body": "モデル最適化が完了したため、Experiment 0053で実際のウェブカメラを用いたエンドツーエンド（E2E）パイプラインの特性評価が行われました。 Experiment 0053aでは、1280x720 30FPSのカメラ入力から30フレームを処理。最新フレームのみを処理する「Latest-frame policy」（506フレームを意図的にドロップ）により、キャプチャ鮮度（freshness）のp95は `66.9600 ms` に抑制されました。非推論オーバーヘッドのmedianは `45.5877 ms`、アプリケーション取り込みからUI送信までのmedianは `609.8091 ms`。しかし、全体の処理FPSは `1.66517 FPS` に留まり、目標の1.8 FPSを満たしませんでした。原因はモデル推論部分が `539.2729 ms` と、スタンドアロンでの `476.360 ms` から大きく劣化（約13.2%の遅化、差 `+62.913 ms`）したことにありました。\n\nこの遅化の原因を突き止めるため、Experiment 0053bで因果関係を監査しました。カメラを閉じた状態（`525.020313 ms`）、オープンアイドリング（`522.476188 ms`）、アクティブキャプチャ（`541.643583 ms`）、フル稼働（`538.634104 ms`）を同一プロセスで比較しましたが、事前登録の10 msの閾値と4ラウンド一貫性を満たす要因（material slowdown components）はゼロでした（Open-idle差 `-2.544125 ms`、Capture差 `+19.167396 ms`、Display差 `-3.009479 ms`、Full-live差 `+13.613791 ms`）。 さらにExperiment 0053cにおいて、完全にクリーンな別プロセス（Pristine）でOpenCVすらインポートせずに計測しても `523.039521 ms` を記録し、インポートのみ（`528.701501 ms`）、初期化後クローズ（`519.576417 ms`）、ビルド中アクティブ（`523.452729 ms`）と大きな差はありませんでした。結果として、476 msから約523 msへの約47 msの遅延は、カメラやディスプレイの競合ではなく、熱やスケジューラ、メモリ常駐などのOS・システムレベルの「幅広い実行コンテキストの変動（broader run/process/system-state drift）」に帰結すると結論づけられました。"
  },
  {
    "id": "section-11",
    "number": 11,
    "title": "ネイティブ画像前処理パイプラインの構築と完全なゼロコピー（Experiment 0054）",
    "body": "システムの洗練とオーバーヘッド削減のため、ネイティブな画像処理パイプラインの構築が開始されました。 Experiment 0054aでは、AVFoundationからCVPixelBufferを経てMetalテクスチャ（MTLTexture）へ至る経路を確立。1280x720 BGRAで300回のコールバックをドロップ0で完走し、有効FPS `29.928894`、コールバック間隔のp95 `38.793519 ms`（PTS間隔 `33.350000 ms`）を達成。テクスチャのReadyレイテンシp95は `0.051127 ms`、ラップ処理p95は `0.042663 ms` と極めて軽量であり、ネイティブイングレスは全くボトルネックにならないことが証明されました（ただし1920x1080が届く解像度ネゴシエーションのバグがあり修正済み、row byte 5120）。\n\nExperiment 0054bでは、Metalを用いたGPU前処理（リサイズ、レターボックス、正規化）がテストされました。CPUリファレンス（`[3, 800, 1065]`、median `8.252167 ms`）に対し、初期のハードウェアリニアサンプラ実装はMetal median `0.506667 ms`（p95 `0.870834 ms`）と圧倒的な速度を示しましたが、平均絶対誤差 `0.0139`、p99 `0.2244`、最大 `1.1336` となり精度ゲートで致命的な失敗（HARD FAIL）となりました。 原因解明のためのExperiment 0054b-1により、誤差はレターボックスのパディング領域ではなく、パス1の最大誤差が `35/255`（p99 `7/255`）、特に高勾配（Top 1% edge）の領域で誤差（mean abs `0.22416`）が急増していることが判明しました。これは、Metalの単純な2x2バイリニア補間と、Pillowのダウンスケール時にフィルタサポートを広げる分離畳み込み（22-bit fixed-point係数）のセマンティクス差でした。 これを踏まえたExperiment 0054b-2において、Pillow互換のスケール対応セパラブルリサイズと22ビット固定小数点丸めをMetalカーネルとして手動実装した結果、パス1の出力は完全にビット一致（最大誤差 `0.0`）。最終テンソルの平均誤差も `3.21288175294e-06`、p99 `4.76837158203e-07`、最大 `0.0175075531006` まで劇的に低下し、事前ゲートをすべてクリア。処理時間もMetal median `0.729000 ms`、p95 `1.168389 ms` と、Pillow互換セマンティクスを再現しながらCPU比で11.4倍の高速化と3 msゲートの達成を両立させました。\n\n最後にExperiment 0054cシリーズで、CPUを一切介さないテンソルの受け渡し（ゼロコピーブリッジ）が検証されました。 0054c-0では、MLXのMetalストレージからPyTorch MPSへの `copy=False` でのDLPack受け渡し（デバイスID `(8, 0)`）がテストされ、ブリッジ単体のmedianは `0.000750000 ms`（0.75 µs）、p95は `0.000792000 ms`。コンシューマを含めてもp95 `0.398962 ms` と極めて低遅延でした。 0054c-1では、このブリッジを経由したテンソル（リファレンスと正確に同じ値）をフルモデルで推論させ、raw max abs `0`、Top-k 900/900、ペア遅延差median `-0.322125 ms` を記録。 0054c-2では、実際の0054b-2の前処理テンソルを入力しました。入力誤差がごくわずかであっても、logitsの平均誤差 `0.208502054`、boxesの平均誤差 `0.055277061` となり、ランク一致は `741/900` まで低下しましたが、Top-900のメンバーシップは `900/900` を維持し、Same-label min IoU は `0.999897354827` でした。ここでも前処理の境界における順位付けの増幅現象が確認されました。 そして0054c-3において、MLXではなくネイティブなObjective-C++で確保された合成の `MTLStorageModeShared` バッファ（サイズ 10,224,000 bytes）をMLX経由でPyTorch MPSにインポートするテストが実施されました。Torch側から書き込んだ値 `-2222.5` がネイティブおよびMLX側の両方で観測されるという完全なエイリアス証明（Alias proof）が成立し、foreignからMLXへのp95が `0.000417 ms`、Torchへの境界構築p95が `0.00129405 ms`（1.294 µs）という圧倒的なパフォーマンスで、真のゼロコピー・オーナーシップパスが確立されました。"
  },
  {
    "id": "section-12",
    "number": 12,
    "title": "総括と次なる展望",
    "body": "MetalGroundプロジェクトは、モデルの精度を一切妥協しない「Exact Optimization」のフェーズにおいて、初期の `999.116584 ms` から `476.360333 ms` へと2.08倍の推論高速化を成し遂げました。それは単純なライブラリの載せ替えではなく、メモリアクセスパターンのフュージョン、代数的なグラフ最適化、DLPackを通じたゼロコピーの非同期同期バグの解決、さらにはハードウェアサンプラとPillowのセマンティクス乖離の手動修正といった、泥臭くも高度なシステムエンジニアリングの結晶です。また、COCO 5000枚の評価において、`47.97026520 AP` と `47.97103061 AP` という `+0.00076541 AP points` の差で完全な同等性を証明したことは、このアプローチの正しさを証明しています。\n\nしかし、最適化を限界まで追求しても、なお500ミリ秒近い推論レイテンシが残存しており、これ以上の純粋な最適化だけで33.3 ms/frame（30 FPS）に到達することは不可能です（残存ギャップ約15倍）。この厳然たる事実と計測データこそが、次なる研究フェーズ「Temporal Amortization（時間的計算償却：約2 Hzの推論と30 Hzのトラッキングによる補間や近似的再利用）」を導入するための最も強力かつ科学的な論拠となります。純粋な最適化の限界を見極めた本研究は、次世代のインタラクティブなオープンボキャブラリ・ビジョンシステム構築に向けた、揺るぎないベースラインとなるのです。"
  }
];
